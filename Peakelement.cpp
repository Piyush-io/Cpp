#include <iostream>
using namespace std;

class Solution
{
public:
    // Function to find the minimum element in sorted and rotated array.
    int peak(int arr[], int low, int high)
    {
        while (low < high)
        {
            
        }
        
    }
};

int main()
{
     int n;
        cin >> n;
        int a[n];
        for (int i = 0; i < n; ++i)
            cin >> a[i];
        Solution obj;
        cout << obj.peak(a, 0, n - 1) << endl;
}